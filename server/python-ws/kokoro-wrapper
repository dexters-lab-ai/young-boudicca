#!/bin/sh
# Wrapper script for kokoro-tts to ensure proper environment setup

export PYTHONPATH="/app:$PYTHONPATH"

# Activate the virtual environment
if [ -f "/opt/venv/bin/activate" ]; then
    . /opt/venv/bin/activate
fi

# Run kokoro-tts with the Python module
python -m kokoro_tts "$@"
